<snippet>
  <description>Pass matches in str thru transforming function</description>
<content><![CDATA[
${1:str}.replace(/${2:match regex}/${3:gi}, (match) => {
  ${4://transform match in some way e.g. toUpperCase();
  return match;}
});${0}
]]></content>  
  <tabTrigger>str_replace_fn_on_matches</tabTrigger>
  <scope>source.js, source.ts</scope>
</snippet>
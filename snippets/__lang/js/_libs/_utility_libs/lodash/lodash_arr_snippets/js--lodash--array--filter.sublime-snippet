<snippet>
  <description>Ret new arr comprised of all items from given array that fn ret truthy for</description>
<content><![CDATA[
${1/.+/let /}${1:outputArray}${1/.+/ = /}_.filter(${2:['some', 'arr']}, (${3:item, index, origArr}) => {
  ${4://Include all items for which this fn ret truthy in the output arr; exclude any that ret falsy}
});${0}
]]></content>  
  <tabTrigger>_arr_filter</tabTrigger>
  <scope>source.js, source.ts, source.jsx, source.tsx</scope>
</snippet>

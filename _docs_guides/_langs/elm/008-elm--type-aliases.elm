------------------------------------------- TYPE ALIASES -------------------------------------------
type alias Message = String
type alias Msg = String
type alias Str = String
    -- any of these now act identically to String

type alias isValid = Bool

type alias ValidationResult = (IsValid, Msg)

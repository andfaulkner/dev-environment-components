#!/usr/bin/env sh
gem install coderay -v "1.1.1"
gem install rails -v "3.2.12" 

# ************** DEBUGGING **************
# PRY
gem install pry -v "0.10.3"
gem install pry-coolline -v "0.2.5"
gem install pry-debugger -v "0.2.3"
gem install pry-doc -v "0.8.0"
gem install pry-macro -v "1.0.1"
gem install pry-rails -v "0.3.4"
gem install pry-stack_explorer -v "0.4.9.2"
gem install awesome_print -v "1.6.1"
gem install pry-theme -v "1.1.3"
# pry glue-ins
gem install hirb -v "0.7.3"
gem install interactive_editor -v "0.0.10"
gem install awesome_print -v "1.6.1"
gem install awesome_print_colors
# BROWSER
gem install meta_request -v "0.4.0"
gem install better_errors -v 1.1.0
# CONSOLE
gem install fivemat
gem install quiet_assets
#VISUALIZATION
gem install railroady -v 1.4.2

# ********* DOCUMENTATION *************
gem install rdoc -v "4.2.2"

# ********* TESTING **************
gem install rspec -v "3.4.0"
gem install factory_girl -v "4.7.0"
gem install factory_girl_rails -v "4.2.1"
gem install rubocop

# ************ CONTINUOUSLY RUNINNG DEV PROCESSES ************
gem install guard -v "2.13.0"
gem install guard-compat -v "1.2.1"
gem install guard-minitest -v "2.4.4"
gem install guard-rails -v "0.7.2"
gem install guard-rspec -v "4.6.5"
gem install rb-fsevent
gem install listen -v "2.7"
gem install rerun -v "0.10"

gem install zeus

# *********** AUTH *************
gem install oauth2 -v "1.1.0"
gem install devise -v "3.5.6"
gem install warden-oauth2
gem install cancancan -v "1.9.2"

# *********** REST *************
gem install grape
gem install grape-rabl
gem install grape-swagger
gem install grape-swagger-ui #"https://mbrpd:moneybarrewards@bitbucket.org/ackroo/grape-swagger-ui.git"
gem install rack-cors', :require => 'rack/cors
gem install rabl -v "0.12.0"
gem install grape-entity
gem install hashie-forbidden_attributes
# requests to REST APIS
gem install rest-client 

# ********** FRONTEND, ASSETS **********
gem install lodash-rails
gem install jquery-rails
gem install jquery-cookie-rails
gem install jquery-ui-rails
gem install coffee-rails -v "3.2.1"
# gem install jquery-datatables-rails -v 3.1.1' #, github: 'rweng/jquery-datatables-rails.git'    1.12.2
gem install jquery-ui-rails
gem install jquery-cookie-rails
gem install prawn -v "1.3.0"
gem install prawn-table
gem install chartkick
# CSS
gem install sass -v "3.4.22"
gem install sass-rails -v "3.2.6"
gem install compass-rails
gem install bootstrap-sass -v "3.2.0.0"
# FRONTEND BUILD
gem install uglifier -v "1.0.3"
# TEMPLATING
gem install haml
# EXCEL
gem install axlsx
gem install acts_as_xlsx -v "1.0.6"

# ************** DB **************
gem install foreigner
gem install pg -v "0.18.4"
gem install mysql2 -v "0.3.13"
gem install redis -v "3.2.2"
gem install audited-activerecord
gem install squeel -v "1.2.3"

# ********** SCHEDULING **********
gem install delayed_job -v "4.1.1"
gem install delayed_job_active_record -v "4.0.0"
gem install daemons
# Crontab
gem install whenever
gem install ice_cube
#adds selectors and helpers for working with recurring schedules in a Rails app
gem install recurring_select -v "1.0.2"

# *********** SERVERS **************
gem install thin
gem install puma -v "3.4.0"
gem install eventmachine -v "1.2.0.1"
gem install passenger -v "5.0.27"

# *********** UTILS **************
gem install os
gem install fileutils
gem install specific_install

# DATES, TIMES
gem install chronic

# Announcements
gem install starburst

# Build tools
gem install bundler -v "1.11.2"

# Form parsing
gem install nokogiri -v "1.6.7.2"

# Model decorators
gem install draper -v "2.1.0"


gem install thor -v "0.19.1"
gem install yard -v "0.8.7.6"

gem install roo -v "2.3.2"
gem install skylight -v "0.10.3"

gem install will_paginate
gem install rails_12factor
gem install json -v "1.7.7"
gem install rails_config
gem install wongi-engine
gem install rouge

gem install acts_as_paranoid


# rails 3
# gem install anima -v "0.2.0";
# gem install morpher -v "0.2.3";
# gem install mutant -v "0.5.26";
# gem install mutant-rspec -v "0.5.26"


